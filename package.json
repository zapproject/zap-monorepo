{
  "private": true,
  "name": "zapSdk",
  "engines": {
    "node": ">=6.12"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "coveralls": "^3.0.2",
    "ganache-cli": "^6.1.4",
    "lerna": "^2.11.0",
    "npm-run-all": "^4.1.3"
  },
  "scripts": {
    "ganache": "ganache-cli -p 8545 --networkId 5777",
    "build": "./node_modules/typescript/bin/tsc",
    "test": "./node_modules/mocha/bin/mocha  -r ts-node/register test/*.ts --serial",
    "run_test": "run-s build test"
  },
  "dependencies": {
    "web3-utils": "^1.0.0-beta.34",
    "zap_contracts": "git+https://github.com/zapproject/zap-ethereum-api.git#master"
  }
}
