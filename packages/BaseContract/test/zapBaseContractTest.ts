const expect = require('chai')
    .use(require('chai-as-promised'))
    .use(require('chai-bignumber'))
    .expect;
import {BaseContract } from '../src/index';

describe('Zap Base Contract Test', () => {

});
